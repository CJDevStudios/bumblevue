import { style } from '@cjdevstudios/bumbleuix-styles/iftalabel';
import BaseStyle from '@cjdevstudios/bumblevue-core/base/style';

const classes = {
    root: 'p-iftalabel'
};

export default BaseStyle.extend({
    name: 'iftalabel',
    style,
    classes
});
