import { style } from '@cjdevstudios/bumbleuix-styles/iconfield';
import BaseStyle from '@cjdevstudios/bumblevue-core/base/style';

const classes = {
    root: 'p-iconfield'
};

export default BaseStyle.extend({
    name: 'iconfield',
    style,
    classes
});
