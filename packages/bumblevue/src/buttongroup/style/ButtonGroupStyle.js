import { style } from '@cjdevstudios/bumbleuix-styles/buttongroup';
import BaseStyle from '@cjdevstudios/bumblevue-core/base/style';

const classes = {
    root: 'p-buttongroup p-component'
};

export default BaseStyle.extend({
    name: 'buttongroup',
    style,
    classes
});
